name: Matrix

on:
  push:
    branches:
      - master

jobs:
  test-parallel:
    runs-on: ${{matrix.os}}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - run: echo "${{ toJson(matrix) }}"
      - run: echo "${{matrix.os}}"
